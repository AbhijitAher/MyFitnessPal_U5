const express = require("express");
const app = express();
app.use(express.json());

// ---------------------- Controllers ---------------------------

// ----------------------- Models -------------------------------

module.exports = app;
